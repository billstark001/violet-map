{
  "name": "violet-map",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter=@violet-map/server --filter=@violet-map/web --parallel -r run dev",
    "dev:full": "pnpm --filter=!@violet-map/assets --parallel -r run dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}